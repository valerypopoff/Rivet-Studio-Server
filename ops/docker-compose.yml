services:
  proxy:
    image: nginx:alpine
    ports:
      - "${RIVET_PORT:-8080}:80"
    environment:
      - RIVET_PUBLISHED_WORKFLOWS_BASE_PATH=${RIVET_PUBLISHED_WORKFLOWS_BASE_PATH:-/workflows}
      - RIVET_LATEST_WORKFLOWS_BASE_PATH=${RIVET_LATEST_WORKFLOWS_BASE_PATH:-/workflows-last}
    volumes:
      - ./nginx.conf:/etc/nginx/templates/default.conf.template:ro
      - ./normalize-workflow-paths.sh:/normalize-workflow-paths.sh:ro
    entrypoint: ["/bin/sh", "/normalize-workflow-paths.sh"]
    depends_on:
      web:
        condition: service_healthy
      api:
        condition: service_healthy
      executor:
        condition: service_started
    restart: unless-stopped

  web:
    build:
      context: ..
      dockerfile: ops/Dockerfile.web
      args:
        VITE_RIVET_DEBUG_LOGS: ${VITE_RIVET_DEBUG_LOGS:-false}
        RIVET_PUBLISHED_WORKFLOWS_BASE_PATH: ${RIVET_PUBLISHED_WORKFLOWS_BASE_PATH:-/workflows}
        RIVET_LATEST_WORKFLOWS_BASE_PATH: ${RIVET_LATEST_WORKFLOWS_BASE_PATH:-/workflows-last}
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  api:
    build:
      context: ..
      dockerfile: ops/Dockerfile.api
    ports:
      - "${RIVET_API_PORT:-3100}:3100"
    environment:
      - PORT=3100
      - RIVET_WORKSPACE_ROOT=/workspace
      - RIVET_WORKFLOWS_ROOT=/workflows
      - RIVET_APP_DATA_ROOT=/data/rivet-app
      - RIVET_RUNTIME_LIBRARIES_ROOT=/data/runtime-libraries
      - RIVET_PUBLISHED_WORKFLOWS_BASE_PATH=${RIVET_PUBLISHED_WORKFLOWS_BASE_PATH:-/workflows}
      - RIVET_LATEST_WORKFLOWS_BASE_PATH=${RIVET_LATEST_WORKFLOWS_BASE_PATH:-/workflows-last}
      - RIVET_ENV_ALLOWLIST=${RIVET_ENV_ALLOWLIST:-}
      - RIVET_SHELL_ALLOWLIST=${RIVET_SHELL_ALLOWLIST:-}
      - RIVET_EXTRA_ROOTS=${RIVET_EXTRA_ROOTS:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENAI_ORG_ID=${OPENAI_ORG_ID:-}
      - OPENAI_ENDPOINT=${OPENAI_ENDPOINT:-}
    volumes:
      - rivet_workspace:/workspace
      - ${RIVET_WORKFLOWS_HOST_PATH:-../workflows}:/workflows
      - rivet_data:/data/rivet-app
      - ${RIVET_RUNTIME_LIBS_HOST_PATH:-rivet_runtime_libs}:/data/runtime-libraries
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3100/api/config"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  executor:
    build:
      context: ..
      dockerfile: ops/Dockerfile.executor
    environment:
      - HOME=/root
      - RIVET_RUNTIME_LIBRARIES_ROOT=/data/runtime-libraries
    volumes:
      - rivet_data:/root/.local/share/com.ironcladapp.rivet
      - ${RIVET_RUNTIME_LIBS_HOST_PATH:-rivet_runtime_libs}:/data/runtime-libraries
    restart: unless-stopped

volumes:
  rivet_workspace:
    driver: local
  rivet_data:
    driver: local
  rivet_runtime_libs:
    driver: local
